# TODO

- [ ] Inspect `static/css/style.css` for redundancies and overlapping selectors.
- [ ] Add CSS variables (design tokens) for colors/sizes used repeatedly.
- [ ] Consolidate glass input/select/textarea rules into a single consistent system.
- [ ] Merge duplicate `.delete-row-btn` rules into one.
- [ ] Unify focus styling selector scope so all styled controls receive the same focus behavior.
- [ ] Reduce redundant declarations and avoid unnecessary duplication.
- [ ] Keep changes CSS-only (no template updates).
- [x] create a productform.html for products based on the other forms
- [ ] Sanity check: reload key pages and verify styling consistency.

